<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!{{else}}DEVOURED!{{/if}}
	</button>

	<button class="delete-devour" data-id="{{this.id}}">DELETE!</button>
</li>
